# Shell Utility Functions

- `clean_macos_metadata.sh`: Removes macOS metadata files beginning with `._` from the current directory and its subdirectories.
- `file_transfer.sh`: Copies or moves files between directories using `rsync`, with support for progress display and excluded files or folders.